= simple_form_for @garden, url: "#" do |f|
  = f.input :name, label: "Garden Name"
  = f.input :height
  = f.input :width

=link_to "Plants", "#"
=link_to "Add", "#"
%select.category_dropdown
  %option Select a Category
  - @categories.each do |category|
    %option{:value => "#{category.id}"}= category.name

#varieties_list
